#include "QueueArrangement.hpp"

template<class T>
QueueArrangement<T>::QueueArrangement() {
    this->size = 0;
    this->front = 0;
    this->back = 0;
}

template<class T>
void QueueArrangement::insert(T i) {
    return 0;
};

template<class T>
T QueueArrangement::dequeue() {
    return 0;
};

void QueueArrangement::clean() {
    return 0;
};
